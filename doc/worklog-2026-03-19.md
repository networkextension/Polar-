# 2026-03-19 工作日志

## 本次完成内容

- 将 `dashboard` 页面从原来的双栏面板调整为侧边栏模式。
- Markdown 列表改为只显示主题，不再显示上传时间。
- 将 `Passkey 绑定` 与 `退出登录` 操作移动到侧边栏内。
- 为侧边栏增加黑白样式切换按钮，并实现本地持久化。
- 调整 Markdown 预览区样式，减少段落与块元素之间的空白。
- 修改 `editor.html`，保存成功后自动返回 `dashboard`。
- 集成 GeoLite，记录登录 IP 与地理位置，并在侧边栏展示最近登录记录。

## 前端调整

### Dashboard

- 文件：
  - `ui/public/dashboard.html`
  - `ui/public/styles.css`

- 主要变更：
  - 桌面端采用左侧常驻侧边栏，移动端保留抽屉式展开。
  - 新增侧边栏底部操作区，集中放置：
    - 外观切换
    - Passkey 绑定
    - 退出登录
  - 新增“最近登录”区域，展示最近 5 条登录记录。
  - Markdown 记录列表仅展示标题。

### Editor

- 文件：
  - `ui/public/editor.html`

- 主要变更：
  - 新建或编辑 Markdown 保存成功后，统一跳转到 `/dashboard.html`。

## GeoLite 登录位置集成

### 后端配置

- 文件：
  - `cmd/dock/main.go`
  - `internal/app/dock/config.go`
  - `internal/app/dock/app.go`

- 新增配置：
  - 环境变量：`GEOLITE_DB_PATH`
  - 默认路径：`data/GeoLite2-City.mmdb`

### 登录记录

- 文件：
  - `internal/app/dock/store.go`
  - `internal/app/dock/handlers.go`
  - `internal/app/dock/passkey.go`
  - `internal/app/dock/login_geo.go`

- 新增内容：
  - `login_records` 表
  - 登录记录模型 `LoginRecord`
  - 登录记录查询接口 `GET /api/login-history`
  - 登录成功后写入：
    - `ip_address`
    - `country`
    - `region`
    - `city`
    - `login_method`
    - `logged_in_at`

- 覆盖的登录场景：
  - 注册后自动登录
  - 邮箱密码登录
  - Passkey 登录

## 数据库变更

- 新增表：`login_records`
- 新增索引：`idx_login_records_user_id_logged_in_at`

## 验证情况

- 已执行：

```bash
env GOCACHE=/tmp/polar-go-cache go test ./...
```

- 结果：
  - `cmd/dock` 通过
  - `internal/app/dock` 通过

## 后续建议

- 将 GeoLite 数据库文件放置到：
  - `data/GeoLite2-City.mmdb`
- 若后续需要安全提醒，可在登录记录基础上继续扩展：
  - 异地登录提示
  - 最近设备/IP 展示
  - 登录风控策略
